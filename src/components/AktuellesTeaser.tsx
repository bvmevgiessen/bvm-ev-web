import React from 'react';
import HomeAktuellesHeader from './HomeAktuellesHeader';

/**
 * Kompakter, lebendiger "Aktuelles"-Header für die Startseite.
 * Zeigt die neuesten 3 News, 3 Events und 3 Blogs mit automatischem 5-Sekunden-Wechsel,
 * zweigeteiltem Layout (Bild/Illustration links, Text/Autor/Teaser rechts), sanften
 * Übergängen und Vereinsfarben.
 */
export default function AktuellesTeaser() {
  return <HomeAktuellesHeader />;
}